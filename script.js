// JavaScript to trigger the animation when the page loads
window.addEventListener('load', function () {
    const header = document.querySelector('.animation');
    header.classList.add('animation');
});
